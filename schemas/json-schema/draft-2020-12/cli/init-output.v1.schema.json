{
  "$defs": {
    "CliInitResult": {
      "additionalProperties": false,
      "properties": {
        "board_id": {
          "type": "string"
        },
        "board_slug": {
          "type": "string"
        },
        "config_path": {
          "description": "`kanban init` 创建或复用的项目配置路径；数据库仍由 `serve` 独占。",
          "type": [
            "string",
            "null"
          ]
        },
        "created": {
          "description": "本次调用是否修改了项目配置文件。",
          "type": [
            "boolean",
            "null"
          ]
        },
        "db_path": {
          "type": "string"
        }
      },
      "required": [
        "db_path",
        "board_id",
        "board_slug"
      ],
      "type": "object"
    }
  },
  "$id": "urn:kanban-tool:schema:cli:init-output:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "data": {
      "$ref": "#/$defs/CliInitResult"
    }
  },
  "required": [
    "data"
  ],
  "title": "Kanban CLI init output v1",
  "type": "object"
}
