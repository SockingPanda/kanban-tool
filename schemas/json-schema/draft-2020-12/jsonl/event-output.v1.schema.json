{
  "$defs": {
    "EventJsonlData": {
      "additionalProperties": false,
      "properties": {
        "actor": {
          "$ref": "#/$defs/RequiredNullableSchema"
        },
        "board_id": {
          "type": "string"
        },
        "created_at": {
          "format": "int64",
          "type": "integer"
        },
        "event_id": {
          "type": "string"
        },
        "id": {
          "format": "int64",
          "type": "integer"
        },
        "kind": {
          "type": "string"
        },
        "payload": true,
        "run_id": {
          "$ref": "#/$defs/RequiredNullableSchema"
        },
        "task_id": {
          "$ref": "#/$defs/RequiredNullableSchema"
        }
      },
      "required": [
        "id",
        "event_id",
        "board_id",
        "task_id",
        "run_id",
        "kind",
        "actor",
        "payload",
        "created_at"
      ],
      "type": "object"
    },
    "EventJsonlType": {
      "enum": [
        "event"
      ],
      "type": "string"
    },
    "RequiredNullableSchema": {
      "description": "仅供 schema 使用的标记，表示 key 必须存在但可以包含 JSON null。\n\nschemars 会把裸 `Option<T>` 视为可省略，而 `#[schemars(required)]` 会有意移除\nnull 分支。这个透明的非 Option wrapper 保留 wire contract 的两部分。",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "$id": "urn:kanban-tool:schema:jsonl:event-output:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "data": {
      "$ref": "#/$defs/EventJsonlData"
    },
    "type": {
      "$ref": "#/$defs/EventJsonlType"
    }
  },
  "required": [
    "type",
    "data"
  ],
  "title": "Kanban JSONL event output v1",
  "type": "object"
}
