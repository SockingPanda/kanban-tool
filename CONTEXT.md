# kanban-tool 领域语言

这里记录 kanban-tool 跨文档共享的产品领域语言。它只定义概念和边界，不承载实现说明、命令清单、文档索引或历史记录。

## Language

### 工作项与生命周期

**看板（Board）**:
任务的隔离与组织边界；每个任务归属于一个看板。

**任务（Task）**:
看板内拥有生命周期和执行信息的规范工作项。

**状态（Status）**:
任务当前生命周期事实，由 `tasks.status` 表示；它不是看板列。
_Avoid_: 看板列

**生命周期（Lifecycle）**:
状态、允许的转换以及转换所需守卫组成的规则。

**可执行性（Readiness）**:
根据任务规格、排期、依赖和执行计划重新计算出的执行资格；它不是第二套状态。

### 执行权与运行

**认领（Claim）**:
对可执行任务原子取得执行权的事实。

**租约（Lease）**:
认领执行权的时间有效性约束。

**运行实例（Run）**:
一次任务执行对应的运行记录。

**事件（Event）**:
记录领域变化的追加式审计事实。

### 事实与派生

**规范事实（Canonical Fact）**:
由共享 service 持有并作为业务真相的事实。

**派生数据（Derived Data）**:
由规范事实计算、可以删除后重建的数据；它不是业务真相。

**投影（Projection）**:
派生数据中的物化表示，只能从规范事实重建，不能回写规范事实。
_Avoid_: 规范数据
