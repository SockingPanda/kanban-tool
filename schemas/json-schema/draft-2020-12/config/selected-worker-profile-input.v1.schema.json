{
  "$defs": {
    "WorkerFinishPolicy": {
      "enum": [
        "done",
        "review",
        "blocked",
        "ready"
      ],
      "type": "string"
    }
  },
  "$id": "urn:kanban-tool:schema:config:selected-worker-profile-input:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "CLI 选中的单个 `[workers.<profile>]` 小节的严格解码结构。\n其它 profile 小节的选择与 opaque 处理仍属于 adapter 关注点。",
  "properties": {
    "claim_ttl_ms": {
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    },
    "command": {
      "type": [
        "string",
        "null"
      ]
    },
    "heartbeat_interval_ms": {
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    },
    "log_dir": {
      "type": [
        "string",
        "null"
      ]
    },
    "on_failure": {
      "anyOf": [
        {
          "$ref": "#/$defs/WorkerFinishPolicy"
        },
        {
          "type": "null"
        }
      ]
    },
    "on_success": {
      "anyOf": [
        {
          "$ref": "#/$defs/WorkerFinishPolicy"
        },
        {
          "type": "null"
        }
      ]
    }
  },
  "title": "Selected Worker Profile Input v1",
  "type": "object"
}
