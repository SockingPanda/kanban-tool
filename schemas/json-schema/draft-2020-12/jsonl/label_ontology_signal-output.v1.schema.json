{
  "$defs": {
    "AtomPolarity": {
      "enum": [
        "positive",
        "negative"
      ],
      "type": "string"
    },
    "LabelOntologySignalData": {
      "additionalProperties": false,
      "properties": {
        "agent_selected": {
          "type": "boolean"
        },
        "board_id": {
          "type": "string"
        },
        "candidate_atom_kind": {
          "anyOf": [
            {
              "$ref": "#/$defs/OntologyCandidateAtomKind"
            },
            {
              "type": "null"
            }
          ],
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`."
        },
        "candidate_atom_polarity": {
          "anyOf": [
            {
              "$ref": "#/$defs/AtomPolarity"
            },
            {
              "type": "null"
            }
          ],
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`."
        },
        "candidate_content_hash": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "candidate_text": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "closed_at": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "confidence": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "created_at": {
          "format": "int64",
          "type": "integer"
        },
        "final_selected": {
          "type": "boolean"
        },
        "id": {
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/OntologySignalKind"
        },
        "observation_id": {
          "type": "string"
        },
        "proposal": {
          "additionalProperties": true,
          "type": "object"
        },
        "proposed_action": {
          "$ref": "#/$defs/OntologyProposedAction"
        },
        "proposed_label_name": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "proposed_label_name_normalized": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "rationale": {
          "type": "string"
        },
        "related_labels": {
          "items": true,
          "type": "array"
        },
        "reviewed_at": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "signal_key": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/SignalStatus"
        },
        "status_reason": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "suggest_rank": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "suggest_score": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "suggest_state": {
          "anyOf": [
            {
              "$ref": "#/$defs/SuggestState"
            },
            {
              "type": "null"
            }
          ],
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`."
        },
        "superseded_by_signal_id": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "target_label_id": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "target_label_name_snapshot": {
          "description": "A key that is mandatory on the wire while its value may explicitly be JSON `null`.",
          "type": [
            "string",
            "null"
          ]
        },
        "updated_at": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id",
        "observation_id",
        "board_id",
        "kind",
        "status",
        "target_label_id",
        "target_label_name_snapshot",
        "related_labels",
        "proposed_action",
        "candidate_atom_polarity",
        "candidate_atom_kind",
        "candidate_text",
        "candidate_content_hash",
        "proposed_label_name",
        "proposed_label_name_normalized",
        "proposal",
        "agent_selected",
        "suggest_state",
        "suggest_score",
        "suggest_rank",
        "final_selected",
        "rationale",
        "confidence",
        "signal_key",
        "superseded_by_signal_id",
        "status_reason",
        "created_at",
        "updated_at",
        "reviewed_at",
        "closed_at"
      ],
      "type": "object"
    },
    "LabelOntologySignalRecordType": {
      "enum": [
        "label_ontology_signal"
      ],
      "type": "string"
    },
    "OntologyCandidateAtomKind": {
      "enum": [
        "applies_when",
        "positive_example",
        "excludes_when",
        "negative_example"
      ],
      "type": "string"
    },
    "OntologyProposedAction": {
      "enum": [
        "observe",
        "add_positive_atom",
        "add_negative_atom",
        "update_semantics",
        "bootstrap_label",
        "rename_label",
        "split_label",
        "merge_labels"
      ],
      "type": "string"
    },
    "OntologySignalKind": {
      "enum": [
        "false_negative",
        "false_positive",
        "vocabulary_gap",
        "name_issue",
        "boundary_issue",
        "structure_issue"
      ],
      "type": "string"
    },
    "SignalStatus": {
      "enum": [
        "open",
        "confirmed",
        "rejected",
        "superseded",
        "resolved"
      ],
      "type": "string"
    },
    "SuggestState": {
      "enum": [
        "selected",
        "candidate",
        "absent",
        "unavailable"
      ],
      "type": "string"
    }
  },
  "$id": "urn:kanban-tool:schema:jsonl:label_ontology_signal-output:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "data": {
      "$ref": "#/$defs/LabelOntologySignalData"
    },
    "type": {
      "$ref": "#/$defs/LabelOntologySignalRecordType"
    }
  },
  "required": [
    "type",
    "data"
  ],
  "title": "Kanban JSONL label_ontology_signal output v1",
  "type": "object"
}
