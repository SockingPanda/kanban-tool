{
  "$defs": {
    "BoardJsonlData": {
      "additionalProperties": false,
      "properties": {
        "archived_at": {
          "$ref": "#/$defs/RequiredNullableSchema2"
        },
        "created_at": {
          "format": "int64",
          "type": "integer"
        },
        "description": {
          "$ref": "#/$defs/RequiredNullableSchema"
        },
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "slug": {
          "type": "string"
        },
        "updated_at": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "id",
        "slug",
        "name",
        "description",
        "created_at",
        "updated_at",
        "archived_at"
      ],
      "type": "object"
    },
    "BoardJsonlType": {
      "enum": [
        "board"
      ],
      "type": "string"
    },
    "RequiredNullableSchema": {
      "description": "Schema-only marker for a key that must be present but may contain JSON null.\n\nA bare `Option<T>` is considered omittable by schemars, while\n`#[schemars(required)]` deliberately removes its null branch. This\ntransparent non-Option wrapper preserves both halves of the wire contract.",
      "type": [
        "string",
        "null"
      ]
    },
    "RequiredNullableSchema2": {
      "description": "Schema-only marker for a key that must be present but may contain JSON null.\n\nA bare `Option<T>` is considered omittable by schemars, while\n`#[schemars(required)]` deliberately removes its null branch. This\ntransparent non-Option wrapper preserves both halves of the wire contract.",
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    }
  },
  "$id": "urn:kanban-tool:schema:jsonl:board-output:v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "data": {
      "$ref": "#/$defs/BoardJsonlData"
    },
    "type": {
      "$ref": "#/$defs/BoardJsonlType"
    }
  },
  "required": [
    "type",
    "data"
  ],
  "title": "Kanban JSONL board output v1",
  "type": "object"
}
